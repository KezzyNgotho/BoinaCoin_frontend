/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './src/**/*.{js,jsx,ts,tsx}', // Adjust the path as needed
  ],
  darkMode: 'media', // or 'class' if you prefer to use a class for dark mode
  theme: {
    extend: {},
  },
  plugins: [],
};
